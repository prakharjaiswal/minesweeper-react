import styled from 'styled-components';
import {INPUT_ELEMENT_PADDING} from './spacing.constants';

export const Button = styled.button`
    padding: ${INPUT_ELEMENT_PADDING};
`;

export const Select = styled.select`
    padding: ${INPUT_ELEMENT_PADDING};
`;